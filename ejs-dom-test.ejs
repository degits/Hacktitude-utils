<%# This code written for testing dom elements manually %>
    <script defer>
      const elementId = "course-duration";    //give your elementID
      document.addEventListener("DOMContentLoaded", runFun);  //must be callback
      function runFun() {
        try {
          console.log(`Element content =${document.getElementById(elementId).innerHTML}`);
        } catch (error) {
          console.log(`"${elementId}" id not found !!!`);
        }

      }
    </script>
    <%# -- %>
